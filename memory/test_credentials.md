# Test Credentials

Use the following seeded credentials to log in and test the NutriScan premium application:

- **Email**: `test@nutriscan.com`
- **Password**: `password123`
- **Mock Token**: A mock JWT token will be generated dynamically upon signing in.
